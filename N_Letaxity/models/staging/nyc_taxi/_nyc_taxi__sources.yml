version: 2

sources:
  - name: nyc_taxi_batch
    database: nyc_taxi_db
    schema: batch_data
    tables:
      - name: trip_data
        description: "Raw NYC taxi trip data loaded from S3."
      - name: taxi_zone_lookup
        description: "Lookup table for NYC taxi zones."
